let options = {
    title: "Menu",
    width: 100,
    height: 200
};
// let title, width, height;
// title = options.title;
// width = options.width;
// height = options.height;

let {width:w} = options;

console.log(w);